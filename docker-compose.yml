version: "3.9"

services:
  squares-pool:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CI_REGISTRY: "dockerhub.alea.ca"
    ports:
      - "8080:8080"
    environment:
      SERVER_PORT: "8080"
      SQUARES_FIXTURES_BASE_URL: "http://mock01.dev.alea.ca:5000"
